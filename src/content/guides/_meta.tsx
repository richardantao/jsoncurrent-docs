import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "path-lifecycle": "Path Lifecycle Events",
  "middleware-client": "Client-side Middleware",
  "middleware-server": "Server-side Middleware",
  "high-throughput": "High-throughput Routes",
  "progressive-rendering": "Progressive Rendering with React",
};

export default meta;
